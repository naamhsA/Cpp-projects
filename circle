#include <iostream>
#include <cmath>
using namespace std;

int main() {
    cout << "Enter a Radius: ";
    double radius;
    cin >> radius;
    const double pi = 3.141;
    double area = pi * pow(radius, 2);
    cout << "Area: " << area;
    
    return 0;
}
    
